Ok lets go through this failing test. I have decided that i want 2 different versions of the MapToSpritesheet function with different arguements. The first one should be MapToSpritesheet(int cellX = 0, int cellY = 32, int destX = 0, int destY = 64, int length = 8192, int base = 16). cellX and cellY are the coordinates of the fist map cell to be included in the copy, destX and destY are the coordinates to start the copy at in the spritesheet. In real PICO-8 the way this copy works is that the sprite number from each cell is treated as a base 16 mask where the the /16 value is used for the first sprite pixel and the %16 value is used for the second. So here length would dictate how many individual pixels are copied, meaning that an odd number would mean that from the last cell on the first value is taken. The base arguement should determine what base we are using for this calculation.

The second version of this function will be similar, but instead of a 1D copy, it will select a rectangle of cells from the map and copy them to a rectangle on the spritesheet. The arguements for this will be MapToSpritesheet(int cellX = 0, int cellY = 32, int cellW = 128, int cellH = 32, int destX = 0, int destY = 64, int destW = 128, int destH = 64, int base = 16). These rectangles don't have to be the same dimensions or area. The copy should fill to the destination dimensions. If the copy is larger than the destination area then the copy will stop when the area is full, and if the copy is smaller then it will stop when there is nothing left to copy and the remaining pixels in the area will be left unchanged.

Lets first write our test cases for this, then fix the real implementation!